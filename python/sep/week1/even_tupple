# write a program to add only even numbers into the tuple

even_numbers = []
for num in range(1, 41):
    if num % 2 == 0:
        even_numbers.append(num)
even_numbers_tuple = tuple(even_numbers)
print(f"Tuple with even numbers from 1 to 40: {even_numbers_tuple}")
