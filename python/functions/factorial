#to print the factorial
#factorial_
def fact(x):
    result=1
    while x>=1:
        result=result*x
        x=x-1
    return result
x=int(input("enter the number"))
result=fact(x)
print(x,"factorial is",fact(x))
